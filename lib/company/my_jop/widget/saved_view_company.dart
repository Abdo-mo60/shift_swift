import 'package:flutter/material.dart';

class SavedViewCompany extends StatelessWidget {
  const SavedViewCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}